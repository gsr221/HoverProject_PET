#include "Consts.h"

void SetupPins(){
  pinMode(LDIR_PIN, OUTPUT);
  //pinMode(LSTOP_PIN, OUTPUT);
  //pinMode(LBREAK_PIN, OUTPUT);
  //pinMode(LSPEED_PIN, INPUT);

  pinMode(RDIR_PIN, OUTPUT);
  //pinMode(RSTOP_PIN, OUTPUT);
  //pinMode(RBREAK_PIN, OUTPUT);
  //pinMode(RSPEED_PIN, INPUT);
}